{
  "name": "gaggle-nft-api",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check api/index.js && node --check lib/auth.js && node --check lib/db.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.13.1"
  }
}
