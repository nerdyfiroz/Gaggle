{
  "name": "gaggle-nft-api",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node --check server.js && node --check api/index.js && node --check lib/auth.js && node --check lib/db.js",
    "lint": "npm run check",
    "check": "node --check server.js && node --check api/index.js && node --check lib/auth.js && node --check lib/db.js"
  },
  "dependencies": {
    "bcryptjs": "2.4.3",
    "express": "^5.2.1",
    "pg": "8.13.1"
  }
}
